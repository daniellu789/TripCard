<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="tag_list"
        android:title="@string/pref_settings_tag"
        android:defaultValue="0"
        android:entries="@array/pref_list_tags"
        android:entryValues="@array/pref_list_tags_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
     dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="location_list"
        android:title="@string/pref_settings_locations"
        android:defaultValue="0"
        android:entries="@array/pref_list_locations"
        android:entryValues="@array/pref_list_locations_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
